<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shave">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mySOTDActivity"></activity>
        <activity android:name=".SaveMySOTDActivity" />
        <activity android:name=".MergeCategoriesActivity" />
        <activity android:name=".ShaveItemInfoActivity" />
        <activity android:name=".CategoryItemActivity" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".UserActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".CreateCategoryActivity" />
        <activity android:name=".CreateItemActivity" />
    </application>

</manifest>